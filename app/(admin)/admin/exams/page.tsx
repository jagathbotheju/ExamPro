import { AdminExamsTab } from '@/app/_components/admin/admin-exams-tab';

export const metadata = { title: 'Admin — Exams' };

export default function AdminExamsPage() {
  return <AdminExamsTab />;
}
